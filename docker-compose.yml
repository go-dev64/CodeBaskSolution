services:
  db:
    image: postgres:15
    restart: always
    volumes:
      - ./data/db:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
  cache:
    image: redis:7.0.4
    restart: always
    volumes:
      - ./data/cache:/data

  web:
    build: .
    command: ["/home/app/web/wait-for-it.sh", "db:5432", "--",
              "uwsgi", "--ini", "/home/app/web/server/uwsgi/uwsgi.ini"]
    restart: always
    volumes:
      - .:/home/app/web/

    environment:
      - DJANGO_SETTINGS_MODULE=config.settings.prod
      - POSTGRES_DB=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    depends_on:
      - db
      - cache
  
  nginx:
    image: nginx:1.23.1
    restart: always
    volumes:
      - ./server/nginx:/etc/nginx/templates
      - .:/home/app/web/
      
    ports:
      - "80:80"
